mesh Th = square(10, 10, [2*x*pi,2*y*pi]);

fespace Vh(Th, P1);
Vh<complex>  uC=(x+1i*y)*(x+1i*y);

savemesh(Th,"complex_test.msh");

{ 
  ofstream file("complex_test.txt"); 
  for (int j=0;j<uC[].n; j++)
    file << uC[][j] << endl;
}
